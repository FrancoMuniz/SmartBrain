import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end', margin: 10}}>
            <p className='f3 link dim white bold pa3 pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;